# Novel Project Strategy

Longform fiction workflow and recovery governance for Codex.

This export directory is structured for a standalone public repository:

- repository root files are human-facing
- `novel-project-strategy/` is the installable skill package

## What This Skill Focuses On

- stable cross-session recovery for novel projects
- chapter-state and task-state discipline
- manuscript-layer separation
- structured load order instead of full-manuscript-first reading
- coordination between outline, timeline, setting, and prose workflow

## Repository Layout

- `novel-project-strategy/`
  - installable public skill package
- `README.md`
  - public landing page for humans

## Installation

When this export is published as its own repository, install the skill from the `novel-project-strategy/` path in that repository.

Example installer inputs:

- repo: `wgwtest/novel-project-strategy`
- path: `novel-project-strategy`

## Maintainer Rule

This export is generated from a separate source-of-truth repository. Update that source first, then sync the public-repo export before publishing or tagging.
